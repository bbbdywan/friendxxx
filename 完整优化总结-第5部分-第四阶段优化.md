# 完整优化总结 - 第5部分：第四阶段 - 接口限流

## 📋 阶段概述

**目标**：使用 Redis 实现接口限流，防止恶意请求和系统过载

**任务数**：2 个任务

**预计耗时**：2 小时

**预期效果**：防止恶意请求，保护服务器资源，系统稳定性提升

---

## 核心思路

### 为什么需要限流？

**没有限流的风险**：
```
恶意用户疯狂请求
  ↓
服务器资源耗尽
  ↓
正常用户无法访问
  ↓
系统崩溃
```

**限流的作用**：
```
检测请求频率
  ↓
超过阈值？
  ↓ 是
拒绝请求（429）
  ↓
保护服务器资源
```

### 限流算法选择

**常见限流算法**：

1. **固定窗口**：
   - 简单，但有临界问题
   - 例如：1秒内最多10个请求
   - 问题：0.9秒10个 + 1.1秒10个 = 0.2秒内20个

2. **滑动窗口**：
   - 更精确，无临界问题
   - 使用 Redis 的 INCR + EXPIRE
   - 我们选择这个 ✅

3. **令牌桶**：
   - 支持突发流量
   - 实现复杂

4. **漏桶**：
   - 平滑流量
   - 实现复杂

**选择滑动窗口的原因**：
- 实现简单（Redis INCR + EXPIRE）
- 精确度高
- 性能好
- 适合 2核2G 服务器

---

## ✅ 任务14：创建限流拦截器

### 问题分析

**需要实现**：
- 使用 Redis 实现滑动窗口限流
- 每个用户每秒最多 N 个请求
- 超过限制返回 429 状态码
- 支持按用户ID或IP限流

### 优化方案

**创建 `RateLimitInterceptor.java`**：

```java
package com.xzh.friendxxx.interceptor;

import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.stereotype.Component;
import org.springframework.web.servlet.HandlerInterceptor;

import java.util.concurrent.TimeUnit;

/**
 * 限流拦截器（任务14）
 * 使用 Redis 实现滑动窗口限流，防止恶意请求
 * 根据压测结果优化：每个用户每秒最多 50 个请求
 */
@Component
@Slf4j
public class RateLimitInterceptor implements HandlerInterceptor {

    @Autowired
    private RedisTemplate<String, String> redisTemplate;

    /**
     * 每秒最大请求数
     * 根据压测结果调整：50线程时 QPS=25，CPU只有5%
     * 可以支持更高的并发，设置为 50 次/秒
     */
    private static final int MAX_REQUESTS_PER_SECOND = 50;

    /**
     * 时间窗口（秒）
     */
    private static final int TIME_WINDOW_SECONDS = 1;

    @Override
    public boolean preHandle(HttpServletRequest request,
                            HttpServletResponse response,
                            Object handler) throws Exception {
        // 获取用户标识（优先使用 userId，其次使用 IP）
        String userId = getUserId(request);
        String clientIp = getClientIp(request);
        String identifier = userId != null ? "user:" + userId : "ip:" + clientIp;

        // Redis key
        String redisKey = "rate_limit:" + identifier;

        try {
            // 使用 Redis 的 INCR 命令实现计数
            Long count = redisTemplate.opsForValue().increment(redisKey, 1);

            if (count == null) {
                count = 1L;
            }

            // 第一次请求时设置过期时间
            if (count == 1) {
                redisTemplate.expire(redisKey, TIME_WINDOW_SECONDS, TimeUnit.SECONDS);
            }

            // 检查是否超过限制
            if (count > MAX_REQUESTS_PER_SECOND) {
                log.warn("限流触发: 标识={}, 请求数={}, URI={}",
                        identifier, count, request.getRequestURI());

                // 返回 429 状态码
                response.setStatus(429);
                response.setContentType("application/json;charset=UTF-8");
                response.getWriter().write(
                    "{\"code\":429,\"message\":\"请求过于频繁，请稍后再试\"}"
                );
                return false;
            }

            log.debug("限流检查通过: 标识={}, 请求数={}/{}",
                     identifier, count, MAX_REQUESTS_PER_SECOND);
            return true;

        } catch (Exception e) {
            log.error("限流检查异常: 标识={}", identifier, e);
            // 异常时放行，避免影响正常业务
            return true;
        }
    }

    /**
     * 从请求中获取用户ID
     */
    private String getUserId(HttpServletRequest request) {
        // 优先从 session 中获取
        Object userIdObj = request.getSession().getAttribute("userId");
        if (userIdObj != null) {
            return userIdObj.toString();
        }

        // 其次从请求头中获取
        String userId = request.getHeader("userId");
        if (userId != null && !userId.isEmpty()) {
            return userId;
        }

        // 最后从请求参数中获取
        userId = request.getParameter("userId");
        return userId;
    }

    /**
     * 获取客户端真实IP
     */
    private String getClientIp(HttpServletRequest request) {
        String ip = request.getHeader("X-Forwarded-For");
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getHeader("Proxy-Client-IP");
        }
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getHeader("WL-Proxy-Client-IP");
        }
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getHeader("HTTP_CLIENT_IP");
        }
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getHeader("HTTP_X_FORWARDED_FOR");
        }
        if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
            ip = request.getRemoteAddr();
        }

        // 处理多个IP的情况（取第一个）
        if (ip != null && ip.contains(",")) {
            ip = ip.split(",")[0].trim();
        }

        return ip;
    }
}
```

### 实现原理

**滑动窗口限流流程**：

```
1. 生成 Redis Key：rate_limit:user:123
2. INCR 计数：count = 1
3. 第一次请求设置过期时间：EXPIRE 1秒
4. 检查计数：count <= 50 → 通过
5. 1秒后 Key 自动过期，计数重置
```

**Redis 命令**：
```redis
# 第1个请求
INCR rate_limit:user:123  → 1
EXPIRE rate_limit:user:123 1  → 1秒后过期

# 第2个请求
INCR rate_limit:user:123  → 2

# 第51个请求
INCR rate_limit:user:123  → 51
返回 429（超过限制）

# 1秒后
Key 自动过期，计数重置为 0
```

### 限流策略

**用户级限流**：
```
已登录用户：按 userId 限流
rate_limit:user:123
```

**IP级限流**：
```
未登录用户：按 IP 限流
rate_limit:ip:192.168.1.100
```

**为什么优先用户ID？**
```
同一用户多设备 → 共享限流配额
防止用户切换设备绕过限流
```

### 限流阈值调整

**初始设置**：10 次/秒
```
保守策略，防止恶意攻击
```

**压测后调整**：50 次/秒
```
根据压测结果：
- 50 线程 → 25 QPS → CPU 5%
- 系统能力远未饱和
- 调整为 50 次/秒更合理
```

**如何选择阈值？**
```
阈值 = 系统最大 QPS × 安全系数
系统最大 QPS ≈ 250（CPU 50%时）
安全系数 = 0.2（留 80% 余量）
阈值 = 250 × 0.2 = 50 次/秒
```

### 优化效果

- ✅ 防止恶意请求
- ✅ 保护服务器资源
- ✅ 支持用户级和IP级限流
- ✅ 异常时自动放行，不影响业务

---

## ✅ 任务15：注册限流拦截器

### 问题分析

**需要实现**：
- 注册限流拦截器到 Spring MVC
- 配置拦截路径
- 排除不需要限流的路径

### 优化方案

**修改 `WebMvcConfiguration.java`**：

```java
package com.xzh.friendxxx.config;

import com.xzh.friendxxx.interceptor.RateLimitInterceptor;
import com.xzh.friendxxx.interceptor.SessionInterceptor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.InterceptorRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

import jakarta.annotation.Resource;

/**
 * 配置类，注册web层相关组件
 */
@Configuration
@Slf4j
public class WebMvcConfiguration implements WebMvcConfigurer {

    @Resource
    private SessionInterceptor sessionInterceptor;

    /**
     * 任务15：注入限流拦截器
     */
    @Resource
    private RateLimitInterceptor rateLimitInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        log.info("开始注册自定义拦截器...");

        // 任务15：注册限流拦截器（优先级最高，先执行限流检查）
        registry.addInterceptor(rateLimitInterceptor)
                .addPathPatterns("/**")
                .excludePathPatterns(
                        // 排除登录、注册接口（避免影响正常登录）
                        "/user/login",
                        "/user/logout",
                        "/user/register",
                        "/user/hrlogin",
                        "/api/user/login",
                        "/api/user/logout",
                        "/api/user/register",
                        "/api/user/hrlogin",
                        // 排除健康检查接口
                        "/health/**",
                        // 排除静态资源
                        "/doc.html",
                        "/doc.html/**",
                        "/webjars/**",
                        "/swagger-resources/**",
                        "/v2/api-docs/**",
                        "/v3/api-docs/**",
                        "/swagger-ui/**",
                        "/swagger-ui.html",
                        "/favicon.ico",
                        "/knife4j/**",
                        "/openapi/**",
                        "/swagger-config",
                        "/api-docs/**",
                        "/css/**",
                        "/js/**",
                        "/images/**",
                        "/fonts/**"
                );

        // 注册 Session 拦截器
        registry.addInterceptor(sessionInterceptor)
                .addPathPatterns("/**")
                .excludePathPatterns(
                        // ... 原有的排除路径
                );
    }
}
```

### 拦截器执行顺序

**拦截器链**：
```
请求进入
  ↓
1. RateLimitInterceptor（限流检查）
  ↓ 通过
2. SessionInterceptor（会话检查）
  ↓ 通过
3. Controller（业务处理）
```

**为什么限流拦截器优先级最高？**
```
限流检查最快（Redis INCR）
尽早拒绝恶意请求
减少后续拦截器和业务逻辑的负担
```

### 排除路径说明

**为什么排除登录接口？**
```
登录是用户的第一个请求
如果限流太严，用户无法登录
影响用户体验
```

**为什么排除静态资源？**
```
静态资源不消耗服务器资源
没有必要限流
减少 Redis 压力
```

**为什么排除健康检查？**
```
健康检查用于监控
需要频繁调用
不应该被限流
```

### 优化效果

- ✅ 限流拦截器优先执行
- ✅ 排除不需要限流的路径
- ✅ 保护业务接口
- ✅ 不影响正常用户体验

---

## 📊 第四阶段优化效果总结

### 限流效果

**防止恶意攻击**：
```
恶意用户每秒发送 100 个请求
  ↓
限流拦截器检测到超过 50 次/秒
  ↓
拒绝后 50 个请求（返回 429）
  ↓
保护服务器资源
```

**正常用户不受影响**：
```
正常用户每秒 1-5 个请求
  ↓
远低于 50 次/秒的限制
  ↓
正常通过
```

### 性能影响

**限流检查性能**：
```
Redis INCR 操作：< 1ms
对请求响应时间影响：< 1%
可以忽略不计
```

**Redis 压力**：
```
1000 并发用户
每秒 1000 个 INCR 操作
Redis 可以轻松处理（10万+ QPS）
```

### 代码变更

**新建文件**：
- `RateLimitInterceptor.java` - 限流拦截器

**修改文件**：
- `WebMvcConfiguration.java` - 注册限流拦截器

---

## 🔄 部署步骤

### 1. 确保 Redis 运行

```bash
# 检查 Redis 状态
docker ps | grep redis

# 如果没有运行，启动 Redis
docker start redis
```

### 2. 重新编译

```bash
mvn clean package -DskipTests
```

### 3. 部署

```bash
# 停止旧应用
./stop.sh

# 上传新 jar 包
# scp target/friendxxx.jar user@server:/path/

# 启动新应用
./start.sh
```

### 4. 验证

```bash
# 测试限流是否生效
# 使用 JMeter 或 curl 快速发送请求

# 发送 60 个请求（超过 50 次/秒限制）
for i in {1..60}; do
  curl http://服务器IP:8080/api/user/info &
done

# 预期结果：
# 前 50 个请求成功（200）
# 后 10 个请求被限流（429）
```

---

## ⚠️ 注意事项

### 1. 限流阈值调整

**根据实际情况调整**：
```java
// 开发环境：宽松限制
private static final int MAX_REQUESTS_PER_SECOND = 100;

// 生产环境：严格限制
private static final int MAX_REQUESTS_PER_SECOND = 50;

// 高峰期：更严格限制
private static final int MAX_REQUESTS_PER_SECOND = 30;
```

### 2. Redis 依赖

- 限流依赖 Redis
- 如果 Redis 宕机，限流会失效（自动放行）
- 建议配置 Redis 高可用

### 3. 分布式部署

**单机部署**：
```
限流基于 Redis
多个实例共享限流配额
自动支持分布式
```

**注意事项**：
```
多个实例的限流是累加的
例如：2 个实例，每个 50 次/秒
实际限流：100 次/秒
```

### 4. 白名单机制

**如果需要白名单**：
```java
// 白名单用户不限流
private static final Set<String> WHITE_LIST = Set.of(
    "admin",
    "monitor"
);

if (WHITE_LIST.contains(userId)) {
    return true;  // 不限流
}
```

---

## 📈 性能测试建议

### 测试场景1：限流触发

```
测试步骤：
1. 使用 JMeter 创建 1 个线程
2. 1 秒内发送 60 个请求
3. 观察响应状态码

预期结果：
- 前 50 个请求：200（成功）
- 后 10 个请求：429（限流）
```

### 测试场景2：限流恢复

```
测试步骤：
1. 触发限流（发送 60 个请求）
2. 等待 1 秒
3. 再次发送请求

预期结果：
- 1 秒后限流自动恢复
- 新请求正常通过
```

### 测试场景3：多用户限流

```
测试步骤：
1. 创建 10 个不同用户
2. 每个用户每秒发送 60 个请求
3. 观察限流是否独立

预期结果：
- 每个用户独立限流
- 用户 A 被限流不影响用户 B
```

### 监控指标

```bash
# 查看 Redis 中的限流 Key
redis-cli KEYS "rate_limit:*"

# 查看某个用户的请求计数
redis-cli GET "rate_limit:user:123"

# 查看限流触发日志
tail -f logs/app.log | grep "限流触发"
```

---

## 💡 优化建议

### 1. 动态调整限流阈值

**根据系统负载动态调整**：
```java
// CPU 使用率 < 50%：宽松限制
if (cpuUsage < 0.5) {
    MAX_REQUESTS_PER_SECOND = 100;
}
// CPU 使用率 > 80%：严格限制
else if (cpuUsage > 0.8) {
    MAX_REQUESTS_PER_SECOND = 30;
}
```

### 2. 分级限流

**不同接口不同限制**：
```java
// 查询接口：宽松限制（100 次/秒）
if (uri.contains("/query")) {
    MAX_REQUESTS_PER_SECOND = 100;
}
// 写入接口：严格限制（10 次/秒）
else if (uri.contains("/save")) {
    MAX_REQUESTS_PER_SECOND = 10;
}
```

### 3. 限流降级

**限流触发后的降级策略**：
```java
// 返回缓存数据
if (rateLimitTriggered) {
    return getCachedData();
}

// 返回降级数据
if (rateLimitTriggered) {
    return getDefaultData();
}
```

---

**下一部分：第五阶段 - 缓存优化**
