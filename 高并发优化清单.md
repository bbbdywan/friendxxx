# 2核2G 高并发优化清单

> 项目：friendxxx 社交聊天应用
> 服务器配置：2核2G
> 目标：支撑 500-1000 并发用户

---

## ✅ 优化任务清单

### 第一阶段：基础配置优化（预计耗时：1-2小时）

- [ ] **任务1：JVM 参数调优**
  - 文件：启动脚本或 Dockerfile
  - 目标：设置堆内存 1G，使用 G1GC
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：减少 GC 停顿，提升响应速度

- [ ] **任务2：数据库连接池精简**
  - 文件：`src/main/resources/application.yml`
  - 修改项：
    - `initial-size: 5`
    - `min-idle: 5`
    - `max-active: 20`
    - `stat-view-servlet.enabled: false`
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：减少内存占用 100-200MB

- [ ] **任务3：Redis 连接池优化**
  - 文件：`src/main/resources/application.yml`
  - 修改项：
    - `lettuce.pool.max-active: 8`
    - `lettuce.pool.max-idle: 4`
    - `lettuce.pool.min-idle: 2`
  - 优先级：⭐⭐⭐⭐
  - 预期效果：减少 Redis 连接开销

- [ ] **任务4：Tomcat 线程池调优**
  - 文件：`src/main/resources/application.yml`
  - 修改项：
    - `server.tomcat.threads.max: 50`
    - `server.tomcat.threads.min-spare: 10`
    - `server.tomcat.max-connections: 500`
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：避免线程过多导致上下文切换

- [ ] **任务5：关闭非必要功能**
  - 文件：`src/main/resources/application.yml`
  - 修改项：
    - 关闭 Knife4j（生产环境）
    - 关闭 Elasticsearch 自动配置（如果不用）
    - 关闭 Druid 监控页面
  - 优先级：⭐⭐⭐
  - 预期效果：减少内存占用 50-100MB

---

### 第二阶段：WebSocket 优化（预计耗时：2-3小时）

- [ ] **任务6：WebSocket 连接数限制**
  - 文件：`src/main/java/com/xzh/friendxxx/websocket/websocketserver/server/WebSocketServer.java`
  - 修改项：
    - 添加 `MAX_ONLINE_USERS = 1000` 限制
    - 在 `onOpen` 方法中检查在线人数
    - 超过限制时拒绝连接
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：防止内存溢出

- [ ] **任务7：WebSocket 会话超时设置**
  - 文件：`src/main/java/com/xzh/friendxxx/websocket/websocketserver/server/WebSocketServer.java`
  - 修改项：
    - `session.setMaxIdleTimeout(600000)` // 10分钟
  - 优先级：⭐⭐⭐⭐
  - 预期效果：自动清理僵尸连接

- [ ] **任务8：WebSocket 消息异步处理**
  - 文件：`src/main/java/com/xzh/friendxxx/websocket/websocketserver/server/WebSocketServer.java`
  - 修改项：
    - `onMessage` 方法改为异步处理
    - 使用 `CompletableFuture.runAsync()`
    - 添加消息长度限制（5000字符）
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：避免阻塞 WebSocket 线程

- [ ] **任务9：踢掉重复连接**
  - 文件：`src/main/java/com/xzh/friendxxx/websocket/websocketserver/server/WebSocketServer.java`
  - 修改项：
    - 同一用户重复连接时，关闭旧连接
  - 优先级：⭐⭐⭐
  - 预期效果：防止连接泄漏

---

### 第三阶段：消息持久化异步化（预计耗时：3-4小时）

- [ ] **任务10：添加聊天消息 MQ 配置**
  - 文件：`src/main/java/com/xzh/friendxxx/config/MQConfig.java`
  - 修改项：
    - 添加 `EXCHANGE_CHAT_MESSAGE`
    - 添加 `QUEUE_CHAT_MESSAGE`
    - 添加 `ROUTINGKEY_CHAT_MESSAGE`
    - 创建交换机、队列、绑定关系
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：支持异步消息持久化

- [ ] **任务11：创建消息消费者**
  - 文件：新建 `src/main/java/com/xzh/friendxxx/consumer/ChatMessageConsumer.java`
  - 修改项：
    - 使用 `@RabbitListener` 监听消息队列
    - 异步写入数据库
    - 添加异常处理和重试逻辑
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：消息持久化不阻塞发送

- [ ] **任务12：修改消息保存逻辑**
  - 文件：`src/main/java/com/xzh/friendxxx/websocket/websocketserver/server/WebSocketServer.java`
  - 修改项：
    - `saveChatMessageAndCache` 方法改为发送到 MQ
    - Redis 只缓存最近 100 条消息
    - 设置缓存过期时间 7 天
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：消息发送速度提升 10 倍

- [ ] **任务13：添加 RabbitTemplate 注入**
  - 文件：`src/main/java/com/xzh/friendxxx/websocket/websocketserver/server/WebSocketServer.java`
  - 修改项：
    - 添加 `private static RabbitTemplate rabbitTemplate`
    - 添加 `@Autowired` setter 方法
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：支持发送消息到 MQ

---

### 第四阶段：接口限流（预计耗时：2小时）

- [ ] **任务14：创建限流拦截器**
  - 文件：新建 `src/main/java/com/xzh/friendxxx/interceptor/RateLimitInterceptor.java`
  - 修改项：
    - 使用 Redis 实现滑动窗口限流
    - 每个用户每秒最多 10 个请求
    - 超过限制返回 429 状态码
  - 优先级：⭐⭐⭐⭐
  - 预期效果：防止恶意请求

- [ ] **任务15：注册限流拦截器**
  - 文件：`src/main/java/com/xzh/friendxxx/config/WebMvcConfiguration.java`
  - 修改项：
    - 注入 `RateLimitInterceptor`
    - 添加到拦截器链
    - 排除登录、注册接口
  - 优先级：⭐⭐⭐⭐
  - 预期效果：全局接口限流生效

---

### 第五阶段：缓存优化（预计耗时：2-3小时）

- [ ] **任务16：添加 Guava 依赖**
  - 文件：`pom.xml`
  - 修改项：
    ```xml
    <dependency>
        <groupId>com.google.guava</groupId>
        <artifactId>guava</artifactId>
        <version>32.1.3-jre</version>
    </dependency>
    ```
  - 优先级：⭐⭐⭐⭐
  - 预期效果：支持本地缓存

- [ ] **任务17：用户信息本地缓存**
  - 文件：`src/main/java/com/xzh/friendxxx/service/impl/UserServiceImpl.java`
  - 修改项：
    - 使用 `LoadingCache` 缓存用户信息
    - 最多缓存 1000 个用户
    - 5 分钟过期
    - 更新用户时清除缓存
  - 优先级：⭐⭐⭐⭐
  - 预期效果：减少 80% 数据库查询

- [ ] **任务18：Redis 消息列表限制长度**
  - 文件：`src/main/java/com/xzh/friendxxx/websocket/websocketserver/server/WebSocketServer.java`
  - 修改项：
    - 每个会话最多缓存 100 条消息
    - 使用 `LTRIM` 命令裁剪列表
  - 优先级：⭐⭐⭐
  - 预期效果：减少 Redis 内存占用

---

### 第六阶段：数据库优化（预计耗时：2小时）

- [ ] **任务19：添加数据库索引**
  - 文件：数据库 SQL 脚本
  - 修改项：
    - `chat_message` 表：`(conversation_id, create_time)`
    - `user` 表：`(user_account)`（如果没有）
    - `group_member` 表：`(group_id, user_id)`
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：查询速度提升 5-10 倍

- [ ] **任务20：分页查询优化**
  - 文件：`src/main/java/com/xzh/friendxxx/service/impl/UserServiceImpl.java`
  - 修改项：
    - 使用 `LIMIT offset, size` 替代 PageHelper（如果性能差）
    - 避免深分页（offset > 10000）
  - 优先级：⭐⭐⭐
  - 预期效果：大数据量分页不卡顿

- [ ] **任务21：慢查询日志分析**
  - 文件：MySQL 配置
  - 修改项：
    - 开启慢查询日志
    - `slow_query_log = 1`
    - `long_query_time = 1`（超过1秒记录）
  - 优先级：⭐⭐⭐
  - 预期效果：发现性能瓶颈

---

### 第七阶段：启动脚本优化（预计耗时：30分钟）

- [ ] **任务22：创建启动脚本**
  - 文件：新建 `start.sh`
  - 修改项：
    - 设置 JVM 参数
    - 自动停止旧进程
    - 后台启动
    - 日志输出到文件
  - 优先级：⭐⭐⭐⭐
  - 预期效果：规范化部署流程

- [ ] **任务23：创建停止脚本**
  - 文件：新建 `stop.sh`
  - 修改项：
    - 优雅停机（kill -15）
    - 等待 30 秒后强制停止
  - 优先级：⭐⭐⭐
  - 预期效果：避免数据丢失

---

### 第八阶段：监控与测试（预计耗时：2小时）

- [ ] **任务24：添加健康检查接口**
  - 文件：`src/main/java/com/xzh/friendxxx/controller/HealthController.java`
  - 修改项：
    - 返回 JVM 内存使用
    - 返回在线用户数
    - 返回数据库连接池状态
  - 优先级：⭐⭐⭐
  - 预期效果：实时监控系统状态

- [ ] **任务25：压力测试**
  - 工具：JMeter 或 wrk
  - 测试项：
    - 登录接口 QPS
    - WebSocket 连接数
    - 消息发送 TPS
  - 优先级：⭐⭐⭐⭐⭐
  - 预期效果：验证优化效果

- [ ] **任务26：内存泄漏检测**
  - 工具：JProfiler 或 VisualVM
  - 检测项：
    - 堆内存增长趋势
    - WebSocket 连接是否泄漏
    - 线程数是否异常增长
  - 优先级：⭐⭐⭐⭐
  - 预期效果：确保长期稳定运行

---

## 📊 优化效果预估

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 并发用户数 | 100-200 | 500-1000 | 5倍 |
| WebSocket 连接 | 不限制（易OOM） | 1000 | 稳定 |
| 消息发送延迟 | 50-100ms | 5-10ms | 10倍 |
| 数据库连接数 | 100 | 20 | 节省80% |
| 内存占用 | 1.5-1.8G | 0.8-1.2G | 节省40% |
| QPS | 50-100 | 200-300 | 3倍 |

---

## 🔧 关键配置文件清单

1. `src/main/resources/application.yml` - 数据库、Redis、Tomcat 配置
2. `src/main/java/com/xzh/friendxxx/config/MQConfig.java` - RabbitMQ 配置
3. `src/main/java/com/xzh/friendxxx/websocket/websocketserver/server/WebSocketServer.java` - WebSocket 核心逻辑
4. `src/main/java/com/xzh/friendxxx/service/impl/UserServiceImpl.java` - 用户服务
5. `pom.xml` - 依赖管理

---

## ⚠️ 注意事项

1. **备份数据**：优化前务必备份数据库和代码
2. **灰度发布**：建议先在测试环境验证，再上生产
3. **监控告警**：配置内存、CPU、连接数告警
4. **回滚方案**：保留旧版本代码，出问题快速回滚
5. **文档更新**：优化完成后更新部署文档

---

## 📝 完成标记

- 开始时间：____年____月____日
- 完成时间：____年____月____日
- 优化人员：________________
- 测试结果：________________

---

## 🎯 下一步计划（可选）

- [ ] 引入 Nginx 反向代理和负载均衡
- [ ] 数据库读写分离
- [ ] 聊天消息分表（按月或按用户ID）
- [ ] 引入 CDN 加速静态资源
- [ ] 微服务拆分（用户服务、聊天服务）
