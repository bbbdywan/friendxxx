# 邢书亮｜Java 后端开发工程师

> 本文档依据原简历与当前 LinkChat 项目源码整理。`【待补充】`内容必须由本人确认后填写；未确认的数据、业务规模和工作成果不要虚构。

## 一、一页简历推荐稿

### 基本信息

- 手机：15586498145
- 邮箱：【沿用原简历邮箱，重新导出 PDF 前检查是否完整显示】
- GitHub：https://github.com/bbbdywan
- 求职方向：Java 后端开发工程师
- 意向城市：武汉 / 杭州 / 上海 / 深圳
- 到岗时间：随时到岗

### 工作经历

> 原简历缺少工作经历。既然已经正式工作，这一部分应放在项目经历之前，优先级高于继续增加技术关键词。

**【公司名称】｜Java 后端开发工程师｜【YYYY.MM—至今】**

- 负责【业务系统/模块名称】的需求分析、接口设计、开发、联调及线上问题排查，主要使用【Java / Spring Boot / MySQL / Redis 等真实技术栈】。
- 完成【1 个最有代表性的业务成果】，解决【具体问题】，结果为【可核实的效率、性能、稳定性或业务指标】。
- 参与【代码评审、上线发布、监控告警、故障处理等真实职责】，沉淀【文档、组件或规范】。

> 如果正式工作经历不足半年，也必须如实写；不要再把自己包装成“只有个人项目的应届生”。项目经历负责证明技术深度，工作经历负责证明工程协作与交付能力。

### 教育经历

**江汉大学｜计算机科学与技术（实验班）｜本科｜【起止时间】**

- 人工智能学院，全日制；专业排名前 15%；CET-【等级】。

### 专业技能

- 掌握 Java 核心语法、集合、异常、泛型及并发编程，理解 `synchronized`、CAS、AQS、ThreadLocal 与线程池的使用场景。
- 熟悉 JVM 内存模型、类加载机制、双亲委派、垃圾回收算法及常见收集器，具备基础的问题定位能力。
- 熟悉 Spring Boot、Spring MVC、MyBatis/MyBatis-Plus，理解 IoC、AOP、Bean 生命周期及自动配置机制。
- 熟悉 MySQL 事务、索引、MVCC 与执行计划，能够进行表结构、索引及分页查询设计。
- 熟悉 Redis 常用数据结构、持久化、淘汰策略及缓存模式；具备原子计数、分布式互斥、限流和缓存一致性实践。
- 熟悉 RabbitMQ 消息模型、可靠投递、死信队列和延迟任务；能够通过异步化解耦实时链路与数据库写入。
- 具备 WebSocket、SSE 流式接口开发经验，能够处理断线取消、状态收尾、幂等重放及流式协议解析。
- 熟悉 Docker / Docker Compose、Git、Linux 基础操作及阿里云 OSS；了解 Vue 3、JavaScript 与前后端联调。

### 项目经历

#### LinkChat 社交与 AI 陪伴平台｜Java 后端开发（个人全栈项目）｜2025.07—至今

项目地址：http://mandx.club/　代码地址：https://github.com/bbbdywan

**技术栈：** Java 17、Spring Boot 3.2、Spring WebFlux、MyBatis-Plus、MySQL、Redis、RabbitMQ、WebSocket、SSE、DeepSeek API、阿里云 OSS、Docker Compose、Vue 3

**项目简介：** 面向社交互动和拟人化 AI 陪伴场景的全栈平台，包含用户与动态、实时私聊/群聊、通知、角色化 AI 对话及人设管理等模块；在 2 核 2GB 服务器环境完成容器化部署与资源约束下的稳定性优化。

- **设计 AI 流式对话编排链路：** 基于 WebFlux 对接 DeepSeek 官方流式 API，通过 SSE 向前端增量推送；设计 `start / message_start / message_delta / message_end / usage / done / error` 事件协议，并实现跨数据块增量解析，使模型单轮回复可自然拆分为多个连续气泡。
- **完善生成状态与幂等机制：** 以 `clientMessageId + replyToMessageId` 关联用户消息和 AI 回复，同一请求重试直接重放已生成结果，避免重复调用模型；围绕 `generating / completed / partial / cancelled / failed` 建立状态收尾，在客户端中断、上游异常和落库失败时保留已生成内容并清理残留状态。
- **实现可热更新的角色人设系统：** 将身份、语言风格、背景、互动规则和安全边界拆分存储，提供草稿、隔离预览、发布、历史版本、回滚和审计能力；使用乐观锁处理并发编辑，新会话读取已发布版本，修改人设无需重新部署服务。
- **增强拟人化上下文能力：** 组合近期对话、会话摘要、长期记忆和关系状态构建分层 Prompt；对用户资料、偏好和事件进行异步提取与冲突更新，在控制上下文长度的同时保持跨会话连续性。
- **保障 AI 链路并发与可恢复性：** 使用 Redis `SET NX + TTL` 限制同一用户并发生成，并通过唯一令牌与 Lua compare-and-delete 防止锁误删；Redis 异常时采用 fail-open 保证主链路可用，正常完成、失败和取消场景均释放资源。
- **构建实时消息与异步持久化链路：** 使用 WebSocket 支持私聊和群聊，消息通过 RabbitMQ 异步落库以解耦实时通信与数据库 I/O；MQ 不可用时降级同步写库，并以 Redis 缓存近期聊天记录及原子维护未读数。
- **统一前后端错误契约：** 后端返回真实 HTTP 状态与结构化错误体，提供稳定机器码、字段级校验信息和 traceId；前端仅在真实 401 时清理登录状态，并区分业务错误、网络异常、主动取消及 SSE 非正常中断，提升线上故障定位效率。
- **完成自动化与端到端验证：** 为流式解析、角色版本、长期记忆、幂等、并发锁、取消状态及异常映射编写单元测试和 Reactor cancellation 测试；完成前后端构建及真实 API、数据库、SSE 链路联调。

## 二、项目亮点精简版（版面不足时使用）

如果简历必须严格控制在一页，项目部分保留下面 5 条即可：

- 基于 Spring WebFlux 对接 DeepSeek 流式 API，设计多气泡 SSE 事件协议与跨 chunk 增量解析器，实现 AI 回复的实时渲染和自然分段。
- 使用 `clientMessageId + replyToMessageId` 实现精确幂等重放，并围绕 completed、partial、cancelled、failed 建立生成状态机，正确处理断流、取消和异常后的数据收尾。
- 构建可热更新的角色人设管理系统，支持草稿预览、发布、版本回滚、审计和乐观锁；新版本对后续请求即时生效，无需重新发版。
- 组合近期消息、会话摘要、长期记忆和关系状态生成分层 Prompt，并异步提取用户资料、偏好和事件，增强跨会话连续性与角色一致性。
- 基于 WebSocket + RabbitMQ 实现实时通信与异步持久化，配合 Redis 完成近期消息缓存、未读计数、生成互斥和限流，并通过降级策略提升可用性。

## 三、根据岗位选择的强调方向

### 投递普通 Java 后端岗位

排序建议：WebSocket/RabbitMQ → Redis → AI 流式编排 → MySQL/错误治理 → 人设管理。招聘方首先要确认候选人具备通用后端能力，AI 是加分项而不是唯一标签。

推荐标题：

> LinkChat 社交与 AI 陪伴平台｜Java 后端开发（个人全栈项目）

### 投递 AI 应用后端岗位

排序建议：SSE 流式协议 → 幂等与状态机 → 人设版本化 → 长期记忆 → Redis 并发控制 → 即时通信。

推荐标题：

> LinkChat 拟人化 AI 社交平台｜AI 应用后端开发

## 四、面试时必须能够讲清楚的技术问题

简历中的每一项都可能被追问，至少准备以下问题：

1. 为什么使用 SSE 而不是 WebSocket 承载模型输出？现有系统为什么又同时保留 WebSocket？
2. SSE 数据块边界与业务消息边界为什么不一致？增量解析器如何处理标签被拆到多个 chunk 的情况？
3. 用户收到部分内容后断开连接，数据库为什么应标记 `partial`，而不是简单标记失败？
4. 幂等键如何关联一轮中的多条 AI 气泡？重复请求为什么不会再次调用模型？
5. Redis 锁为什么不能直接 `DEL`？唯一 token + Lua compare-and-delete 解决了什么竞态？
6. 人设草稿、已发布版本、活动版本指针如何设计？发布和回滚如何保证可审计？
7. 长期记忆、近期对话和摘要分别解决什么问题？如何避免上下文无限增长？
8. RabbitMQ 不可用时为什么允许同步落库？这种降级策略有什么代价？
9. 如何区分用户主动停止、网络断开、模型错误和数据库失败？前端应该展示什么？
10. 2 核 2GB 环境下主要限制是什么？线程池、连接池、容器内存和日志应如何配置？

## 五、原简历需要删除或改正的内容

- 删除“Spring AI Alibaba（DashScope + RAG）”及 `qwen-max` 主链路描述。当前代码已改为 DeepSeek 官方 API，继续保留会造成简历与代码不一致。
- 不建议写“深刻理解”“深入了解”“熟练掌握全部原理”等难以证明的措辞，改为“熟悉”“理解”“具备实践”。
- 原项目条目过长，文件命名、缓存 TTL、连接数等细节不应全部挤在一页；这些内容适合放在面试回答中。
- “消息零丢失”属于过强承诺。没有生产级端到端 exactly-once 证据时，应写成“降低消息丢失风险”或“提供 MQ 失败同步落库兜底”。
- “滑动窗口限流”需要与实际实现保持一致。如果代码只是固定窗口 `INCR + EXPIRE`，简历不得写成滑动窗口。
- `Spring Boot 3.2 / Java 17`、测试数量等数字在最终导出前应从构建结果再次核对；测试总数会持续变化，主简历建议写“覆盖核心链路”，不要写易过期的数量。
- 项目网址若暂时无法访问，应删除网址或注明“演示环境”，避免面试官点击后得到错误页面。

## 六、仍需本人补齐的信息

- 教育起止日期、CET 等级和完整邮箱（原 PDF 字体编码导致部分字符无法可靠提取）。
- 正式工作的公司、岗位、入离职时间、真实职责及可以公开的量化成果。
- LinkChat 中本人实际独立负责的模块。若为多人项目，应将“个人全栈项目”改为“团队项目”，并明确个人职责。
- 线上真实用户量、QPS、消息量、响应耗时等数据；没有监控或压测报告时不要写数字。
- GitHub 仓库是否公开，以及线上演示地址是否长期可用。

## 七、排版建议

- 有正式工作经历后，顺序使用：基本信息 → 工作经历 → 项目经历 → 专业技能 → 教育经历。
- 控制在 1 页或最多 2 页；正文使用 10.5—11 pt，项目每条 1—2 行，关键词加粗即可。
- 不放大段“项目简介”，用一句话说明业务，再用 4—6 条“问题—方案—结果”描述个人贡献。
- 电话、邮箱、GitHub 保持可复制文本，导出 PDF 后重新复制检查，避免原文件中数字和邮箱乱码。

