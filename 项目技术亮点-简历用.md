# FriendXXX 社交即时通讯平台 — 项目技术亮点

## 项目概述

基于 Spring Boot 3.2 + Java 17 构建的社交即时通讯单体应用，涵盖实时聊天、社交动态、AI 对话、通知推送等核心功能模块，针对 2核2GB 低配服务器进行了全链路性能调优。

---

## 技术栈

| 分类 | 技术 |
|------|------|
| 核心框架 | Spring Boot 3.2.12 / Java 17 / Jakarta EE |
| ORM | MyBatis-Plus 3.5.9 |
| 数据库 | MySQL + Druid 连接池 |
| 缓存 | Redis（Lettuce 客户端）+ Guava 本地缓存 |
| 消息队列 | RabbitMQ + Spring AMQP |
| 实时通信 | Jakarta WebSocket |
| AI | 阿里云 DashScope（通义千问 qwen3-max）+ Spring AI Alibaba |
| 文件存储 | 阿里云 OSS |
| 文档生成 | iText7（PDF）/ EasyExcel |
| API 文档 | Knife4j / OpenAPI 3 |
| 序列化 | FastJSON2 |
| 工具库 | Apache Commons Lang3 / Lombok |

---

## 一、WebSocket 实时通信系统

### 核心实现
- 基于 Jakarta WebSocket 实现全双工实时通信，端点 `/websocket/{userId}`
- 使用 `ConcurrentHashMap<String, WebSocketServer>` 管理连接，`AtomicInteger` 无锁计数在线人数，支持最大 1000 并发连接
- 会话超时设置为 10 分钟（`session.setMaxIdleTimeout(600000)`），防止僵尸连接占用资源

### 重复连接处理
- 同一用户重新连接时，自动踢掉旧连接（`CloseReason.CloseCodes.NORMAL_CLOSURE`），保证连接唯一性

### 消息类型路由
- 支持 `private`（私聊）和 `group`（群聊）两种消息类型，通过 JSON 中的 `type` 字段路由
- 私聊会话 ID 生成规则：`"private_" + min(userId1, userId2) + "_" + max(userId1, userId2)`，保证双向会话 ID 唯一一致

### 异步消息处理
- 使用 `CompletableFuture.runAsync()` 异步处理收到的消息，避免阻塞 WebSocket 线程
- 消息长度限制 5000 字符，超长消息直接返回错误响应

---

## 二、RabbitMQ 消息队列架构

### 四条消息链路

| 交换机 | 队列 | 用途 |
|--------|------|------|
| EXCHANGE_CHAT_MESSAGE | QUEUE_CHAT_MESSAGE | 聊天消息异步持久化 |
| EXCHANGE_NOTIFICATION | QUEUE_NOTIFICATION | 点赞/评论通知异步处理 |
| EXCHANGE_USER_TTL | QUEUE_USER_REGISTER → QUEUE_USER_DELETE | 用户生命周期延迟删除 |
| EXCHNAGE_DELAY | QUEUE_ORDER → QUEUE_DELAY | 限时动态自动过期删除 |

### 聊天消息异步持久化（MQ 降级兜底）
- 消息发送后立即投递至 `EXCHANGE_CHAT_MESSAGE`，由 `ChatMessageConsumer` 异步写库，解耦实时通信与数据库 IO
- 若 RabbitTemplate 不可用，自动降级为同步写库（`fallbackSaveChatMessage`），保证消息零丢失
- Consumer 写库后同步更新 Redis List 缓存，保留最近 100 条，TTL 7 天

### 死信队列实现延迟任务
- 利用 RabbitMQ Dead Letter Exchange + 消息 TTL 实现延迟删除：
  - **限时动态**：发布动态时设置 `expiration`，过期后路由至死信队列，触发 `socialPostService.remove(deleteTtl)` 自动删除
  - **临时用户**：HR 登录后创建临时账号，30 分钟后通过死信队列自动清理（`userService.removebyAccount`）
- 所有交换机和队列均配置 `durable=true`，Broker 重启后消息不丢失

### 通知消息在线/离线分发
- `NotificationConsumer` 消费通知消息后：
  - 用户在线：直接通过 WebSocket 实时推送
  - 用户离线：存入 Redis List（`notify:offline:{userId}`），最多保留 200 条，TTL 7 天
  - 用户上线时（`@OnOpen`）自动拉取离线通知并推送，清空 Redis 缓存

---

## 三、Redis 多场景应用

| 场景 | Key 设计 | 数据结构 | TTL |
|------|----------|----------|-----|
| 聊天记录缓存 | `message:list_{conversationId}` | List | 7 天 |
| 用户信息缓存 | `message:user_{userId}` | String（JSON） | 30 分钟 |
| 未读消息计数 | `unread:{userId}` | Hash（field=conversationId） | 永久 |
| 点赞状态去重 | `post:like:{postId}` | Set | 永久 |
| 评论缓存 | `post:comment:{postId}` | List | 永久 |
| 用户列表缓存 | `user:list:{page}:{size}` | String（JSON） | 30 分钟 |
| 离线通知 | `notify:offline:{userId}` | List | 7 天 |
| 接口限流计数 | `rate_limit:{user/ip}` | String（INCR） | 1 秒 |

### 关键设计细节
- **未读数**：用 Redis Hash 的 `HINCRBY` 原子操作维护每个会话的未读计数，打开聊天窗口时调用 `HDEL` 清零，O(1) 读写
- **点赞去重**：用 Redis Set 的 `SISMEMBER` 判断是否已点赞，防止重复点赞，数据库使用 `CASE WHEN like_count > 0 THEN like_count - 1 ELSE 0 END` 防止负数
- **评论缓存**：写评论时同步 `rightPush` 到 Redis List；读评论时优先读缓存，缓存未命中再查 MySQL 并回填（Cache-Aside 模式）
- **用户更新缓存失效**：更新用户信息后，通过 `redisTemplate.keys("user:list:*")` 批量删除相关缓存

---

## 四、接口限流（Redis 滑动窗口）

- 基于 Redis INCR + EXPIRE 实现每用户/IP 每秒最多 50 次请求的限流
- 优先级最高的拦截器（`RateLimitInterceptor`），触发后返回 HTTP 429
- 用户标识优先级：Session userId > 请求头 userId > 请求参数 userId > 客户端真实 IP（支持 X-Forwarded-For 代理穿透）
- 限流异常时放行（fail-open），避免 Redis 故障影响正常业务

---

## 五、Session 认证 + ThreadLocal 用户上下文

- 使用 Spring Session + Redis 实现分布式 Session 管理，Session TTL 30 分钟（滑动过期）
- `SessionInterceptor` 拦截所有需要认证的接口，校验 Session 中的 `userId`，失败返回 401
- 通过 `BaseContext`（ThreadLocal）在请求生命周期内传递当前用户 ID，`afterCompletion` 中清理防止内存泄漏
- 拦截器注册顺序：`RateLimitInterceptor`（order=1）> `SessionInterceptor`（order=2）

---

## 六、社交动态模块

### 功能列表
- 发布/编辑/删除动态，支持图片上传（阿里云 OSS）
- 点赞/取消点赞（Redis Set 去重 + 数据库原子更新）
- 评论（MySQL 持久化 + Redis 缓存 + MQ 异步通知）
- 限时动态（发布时设置 TTL，通过死信队列自动过期删除）

### 自定义 TypeHandler
- `ListToJsonTypeHandler`：将 `List<String>`（图片列表）与数据库 JSON 字段互转
- `MoodTypeHandler`：将 `List<Mood>`（心情标签）与数据库 JSON 字段互转
- 两个 TypeHandler 均基于 Jackson ObjectMapper，注册到 MyBatis 类型系统

### 事务 + MQ 一致性
- 发布动态使用 `@Transactional`，通过 `TransactionSynchronizationManager.registerSynchronization` 在事务提交后才发送 MQ 消息，避免事务回滚但消息已发出的问题

### 软删除
- 所有实体（User、SocialPost、Comment、UserNotification）均使用 `@TableLogic` 软删除，`is_deleted` 字段标记，支持数据恢复和审计

---

## 七、通知系统

- `user_notification` 表记录点赞/评论互动，字段：`to_user_id`、`from_user_id`、`from_nickname`、`type`、`post_id`、`content`、`is_read`
- 复合索引 `(to_user_id, is_deleted, create_time)` 优化分页查询，`(to_user_id, is_read, is_deleted)` 优化未读数统计
- 防自通知：点赞/评论时判断 `author.userId != operatorUserId`，不给自己发通知
- 支持全部已读（`UPDATE ... SET is_read=1 WHERE to_user_id=?`）和单条已读

---

## 八、AI 对话功能

- 接入阿里云 DashScope（`qwen3-max-2026-01-23`），通过 Spring AI Alibaba 封装
- 自定义 `SoftDeleteChatMemoryRepository` 继承 `JdbcChatMemoryRepository`，重写 SQL 加入 `is_deleted=0` 过滤，实现基于 MySQL 的多轮对话上下文记忆（软删除支持）
- 用户可自定义 Prompt（`user_prompt` 表），支持多条 Prompt 管理，`setActive` 切换当前激活的 Prompt
- AI 配置：temperature=0.7，top-p=0.8，默认系统人设可在 `AIConfig` 中配置

---

## 九、Guava 本地缓存（两级缓存）

- `UserServiceImpl` 中使用 Guava `LoadingCache` 缓存用户信息：最多 1000 条，5 分钟过期，开启 `recordStats()` 监控命中率
- 更新用户时调用 `userCache.invalidate(userId)` 主动失效，保证缓存一致性
- 提供 `getCacheStats()` 接口暴露缓存统计，便于监控

---

## 十、群聊功能

- 创建群聊（群名唯一校验）、加入群聊、获取群聊列表（含当前用户是否已加入标识）
- 群聊消息会话 ID 格式：`group_{groupId}`，通过 `GroupMemberService.getGroupMemberIds` 获取成员列表，遍历推送

---

## 十一、PDF 动态生成

- 使用 iText7 在内存中生成 PDF（`ByteArrayOutputStream`），支持中文字体（STSong-Light + UniGB-UCS2-H）
- 提供下载（`Content-Disposition: attachment`）、在线预览（`Content-Disposition: inline`）、Base64 编码三种返回方式

---

## 十二、性能调优（针对 2核2GB 服务器）

| 组件 | 参数 | 值 |
|------|------|----|
| Tomcat | max-threads | 50 |
| Tomcat | min-spare-threads | 10 |
| Tomcat | max-connections | 500 |
| Druid | initial-size / min-idle | 5 |
| Druid | max-active | 20 |
| Redis Lettuce | max-active | 8 |
| Redis Lettuce | max-idle | 4 |
| WebSocket | MAX_ONLINE_USERS | 1000 |
| 分页 | 深分页保护 | offset > 10000 时拒绝请求 |
| Knife4j | 生产环境 | 关闭（节省内存） |

---

## 十三、API 设计规范

- 统一响应体 `Result<T>`，包含 code、message、data 字段
- 全局异常处理 `GlobalExceptionHandler`，统一捕获 `BusinessException` 和系统异常
- 自定义错误码 `ErrorCode` 枚举，业务异常携带明确错误信息
- 接口文档使用 Knife4j + OpenAPI 3，`@Tag` / `@Operation` 注解描述模块和接口

---

## 简历描述参考

> 设计并实现基于 WebSocket + RabbitMQ 的实时消息系统：消息发送后异步投递至 MQ 持久化，解耦实时通信与数据库 IO；设计 MQ 不可用时的同步降级兜底机制，保证消息零丢失；利用 Redis Hash 维护会话未读计数，用户上线后自动推送离线通知。

> 基于 RabbitMQ 死信队列 + TTL 实现限时动态自动过期删除和临时用户定时清理，事务提交后才发送 MQ 消息，保证事务与消息的一致性。

> 使用 Redis Set 实现点赞去重，Cache-Aside 模式缓存评论列表，Guava LoadingCache 作为用户信息本地二级缓存；基于 Redis INCR 实现滑动窗口接口限流（50次/秒），支持用户 ID 和 IP 双维度限流。

> 接入阿里云 DashScope 大模型，自定义 JdbcChatMemoryRepository 实现基于 MySQL 的多轮对话上下文记忆（软删除支持），支持用户自定义 Prompt 并动态切换。

> 针对 2核2GB 服务器进行全链路调优：Tomcat 线程池（50线程）、Druid 连接池（20连接）、Redis 连接池（8连接）、WebSocket 最大 1000 并发连接，并设置深分页保护（offset > 10000 拒绝）。
